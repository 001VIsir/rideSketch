# 应用配置
spring.application.name=rideSketch
server.port=8080

# MySQL配置
spring.datasource.url=jdbc:mysql://localhost:3306/ridesketch?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=Cqian1231
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# MyBatis配置
mybatis.mapper-locations=classpath:mapper/**/*.xml
mybatis.configuration.map-underscore-to-camel-case=true
mybatis.configuration.log-impl=org.apache.ibatis.logging.stdout.StdOutImpl

# Redis配置
spring.data.redis.host=localhost
spring.data.redis.port=6379
spring.data.redis.password=
spring.data.redis.timeout=3000ms
spring.data.redis.lettuce.pool.max-active=8
spring.data.redis.lettuce.pool.max-idle=8
spring.data.redis.lettuce.pool.min-idle=0

# JWT配置
jwt.secret=rideSketch2026SecretKeyForJWTTokenGeneration
jwt.expiration=86400000

# 高德地图配置
amap.key=30df485f0872725106bacd290344efd5
amap.security-key=

# Spring AI Ollama配置
spring.ai.ollama.base-url=http://localhost:11434
spring.ai.ollama.chat.options.model=qwen3:8b
spring.ai.ollama.chat.options.temperature=0.7

# 兼容旧版Ollama配置
ollama.base-url=http://localhost:11434
ollama.model=qwen3:8b

# 日志配置
logging.level.org.example.ridesketch=debug
logging.level.org.springframework.web=info
